# SchoolPay Integration (Optional)
# Base URL for SchoolPay API
SCHOOLPAY_BASE_URL=https://schoolpay.co.ug/paymentapi

# Note: School-specific credentials (school_code, api_password) are configured
# per school via the admin panel at /api/v1/schoolpay/config
# Each school can have different SchoolPay credentials
